// /** @type {import('next').NextConfig} */
// const nextConfig = { devIndicators: false };

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
};
export default nextConfig;
